FAVORITES LIST

To open the window, just navigate to Tools -> Favorites List.
You can create multiple instances.
It support scene objects (like GameObjects) as well as project objects (like folders, scripts or prefabs).
